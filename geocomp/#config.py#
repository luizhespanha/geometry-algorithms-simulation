#!/usr/bin/env python
# -*- coding: utf-8 -*-

# tamanho da area de desenho
# WIDTH = 500
WIDTH = 600
# HEIGHT = 500
HEIGHT = 600

# delay entre um passo e outro (em milissegundos)
DELAY = 50

# maximo valor para DELAY (em milissegundos)
MAX_DELAY = 1000

# diretorio onde estao os arquivos de entrada
DATADIR = 'dados'

# largura de uma linha
LINEWIDTH = 2
# largura de uma linha vertical/horizontal (linha de varredura,
#   linha que indica onde foi feita a divisao em um algoritmo
#   de divisao e conquista, ...)
LINEWIDTH_SPECIAL = 1

# um ponto é desenhado como um circulo
# raio desse circulo para um ponto normal e em destaque
#RADIUS = 2
RADIUS = 3
RADIUS_HILIGHT = 5

# cores usadas

# cor de um ponto normal e em destaque
# COLOR_POINT = 'blue'
COLOR_POINT = 'white'
COLOR_HI_POINT = 'green'

# cor padrao de uma linha
COLOR_LINE = 'red'

# cor de uma linha vertical/horizontal (linha de varredura,
#   linha que indica onde foi feita a divisao em um algoritmo
#   de divisao e conquista, ...)
COLOR_LINE_SPECIAL = 'green'

# cores de um poligono normal e em destaque
COLOR_POLYGON = 'red'
COLOR_HI_POLYGON = 'green'
COLOR_HI_POLYGON_POINT = 'red'

# cores de um segmento de reta normal e em destaque
COLOR_SEGMENT = 'red'
COLOR_HI_SEGMENT = 'green'
COLOR_HI_SEGMENT_POINT = 'red'

# cor em que uma primitiva é desenhada
COLOR_PRIM = 'yellow'

# miscelania:
# cor usada para:
#   mostrar pares antipodas em farthest.diameter
#   mostrar a qual vertice um ponto esta associado em convexhull.incr_prob
#   mostrar segmentos tangentes em convexhull.mergehull
COLOR_ALT1 = 'cyan'

#   mostrar o baricento do triangulo original em convexhull.incr_prob
COLOR_ALT2 = 'light blue'

#   mostrar o ponto que esta sendo considerado em convexhull.incr_prob
#   mostrar o ponto que esta sendo considerado em convexhull.mergehull
COLOR_ALT3 = 'magenta'

COLOR_ALT4 = 'green'

COLOR_ALT5 = 'orange'


# --------------------------------------------
# nao mexa daqui para baixo
# --------------------------------------------
if WIDTH <= 0: WIDTH = 500
if HEIGHT <= 0: HEIGHT = 500
if MAX_DELAY < 0: MAX_DELAY = 1000
if DELAY > MAX_DELAY: DELAY = MAX_DELAY
if LINEWIDTH <= 0: LINEWIDTH = 1
if RADIUS <= 0: RADIUS = 2
if RADIUS_HILIGHT <= 0: RADIUS_HILIGHT = 5
